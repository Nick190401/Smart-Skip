; Smart Skip v2 — PHP-FPM runtime settings for shared hosting (Hostinger)
; Place this file in the same directory as auth.php / api.php.
; Changes take effect within 5 minutes (PHP_INI_PERDIR cache TTL).

; Raise upload cap to fit 5 MB attachments + multipart envelope overhead
upload_max_filesize = 6M
post_max_size       = 7M
max_file_uploads    = 5
