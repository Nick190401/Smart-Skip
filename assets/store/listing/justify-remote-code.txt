No remote code is used. Every script that runs is packaged in the extension. content/page-interceptor.js is injected into the page context from the extension's own package via chrome.runtime.getURL() and is declared under web_accessible_resources. The extension's server returns JSON data only — configuration flags, learned CSS selectors and segment timestamps — which is parsed as data and never evaluated as code.
